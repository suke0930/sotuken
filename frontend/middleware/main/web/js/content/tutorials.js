// Tutorials Page Content in Markdown

export const tutorialsContent = `
# Minecraft Server Manager 操作ガイド（WebUI）

このページは、ミドルウェア経由で WebUI を使い、Minecraft サーバーを作成・運用するための手順をまとめたものです。

---

## 1. 画面構成（まずここだけ）

### 上部バー（ナビゲーションバー）
- 通知ベル: サーバーの起動/停止/クラッシュ/自動再起動などのイベント通知を表示します（既読・全削除も可能）。
- テーマ切替: ライト/ダークを切り替えます（ブラウザに保存されます）。
- ユーザーメニュー: ログアウトができます。

### 左側メニュー（サイドバー）
- サーバー一覧: 作成済みサーバーの状態確認と操作。
- 新規作成: 新しいサーバーの作成。
- JDK管理: インストール済み JDK の確認・削除（使用中は削除不可）。
- システム設定: 認証が有効かどうかなどの簡易テスト。
- ダウンロード管理: サーバー Jar / JDK を手動で取得・進捗確認（通常は作成時に自動で行われます）。
- FRP管理（任意）: サーバーを外部公開するためのトンネル管理。
- About / Tutorials: このページを含む補足コンテンツ。

---

## 2. 初回ログイン / 2回目以降

1. 初回アクセス時は「ユーザー登録（Signup）」が表示されます。
2. 以降は「ログイン（Login）」でサインインします。
3. ログアウトは右上のユーザーメニューから行います。

補足:
- この WebUI は単一ユーザー運用を想定しています。ID/パスワードは忘れないように管理してください。

---

## 3. サーバーを作成する（新規作成）

サイドバーの「新規作成」から、以下を順に設定します。

### 入力項目の意味
- サーバー名（必須）: 一覧で識別するための名前です。同じ名前は使えません。
  - 入力欄の **?** から命名ルールのヘルプを確認できます。
- メモ: 運用メモ（任意）。
- サーバーソフトウェア（必須）: サーバー種別を選びます（例: Vanilla / Paper / Spigot など）。
- Minecraft バージョン（必須）: 選んだソフトウェアで利用可能なバージョンから選択します。
- JDK バージョン（自動）: バージョン選択に応じて自動で設定されます（未インストールの場合は作成時に自動で導入されます）。
- ポート番号（必須）: 1024〜65535 の範囲で指定します。既に別サーバーが使用中のポートは警告が出ます。
- メモリ（最小/最大）: サーバーに割り当てるメモリです。プレイヤー数やプラグイン数に合わせて調整してください。

### 作成を実行
「サーバーを作成」を押すと、モーダルで進捗とログが表示されます。
- ダウンロードが発生する場合は、操作ごとに進捗（%）が表示されます。
- 失敗した場合は「再試行」からやり直せます。

---

## 4. サーバー一覧でできること

「サーバー一覧」には、各サーバーの状態と基本情報（ソフトウェア/バージョン/JDK/ポート/メモリ/作成日）が表示されます。

### 状態（ステータス）
- 稼働中: サーバーが起動しています。
- 停止中: サーバーが停止しています。
- Crash!: 異常終了した状態です（「再起動」で復帰を試してください）。

### 主な操作ボタン
- 起動 / 停止: サーバーの起動・停止。
- コンソール: ログ閲覧とコマンド送信。
- プロパティ: \`server.properties\` の GUI/テキスト編集。
- 編集: 起動設定（ポート/メモリ/引数/自動再起動）を編集。
- 削除: サーバーを完全に削除（停止中/Crash のときのみ）。

> 注意: 削除はワールドを含むサーバーデータを削除します。取り消せません。

---

## 5. サーバー設定を編集する（編集ボタン）

サーバーカードの「編集」から、次の項目を変更できます。
- サーバー名 / メモ
- ポート番号
- 最小/最大メモリ
- JVM Arguments: Java の起動オプション（スペース区切りで指定）
- Server Arguments: サーバー側の起動オプション（スペース区切りで指定）
- 自動再起動: クラッシュ時に自動再起動します
  - 最大連続再起動回数: 連続で再起動できる上限
  - リセット時間（秒）: この時間だけ安定稼働すると「連続回数」をリセットします

補足:
- 一部の変更は「次回起動時に反映」されます。確実に反映させたい場合は、停止→起動（再起動）してください。

---

## 6. コンソールを使う（コンソールボタン）

コンソールでは、サーバーログをリアルタイムに見ながらコマンドを送れます。

### 便利な機能
- タイムスタンプ表示の ON/OFF: \`[01:23:45 INFO]:\` のようなプレフィックスを非表示にできます。
- 自動スクロール: 新しいログに追従します（上にスクロールすると自動で OFF になります）。
- ログクリア: 保存済みログも含めてクリアします（取り消せません）。

### よく使うコマンド例
| コマンド | 説明 |
|---|---|
| \`list\` | オンラインプレイヤー一覧 |
| \`stop\` | サーバーを終了 |
| \`whitelist add <name>\` | ホワイトリストに追加 |
| \`op <name>\` | OP 権限を付与 |

---

## 7. サーバープロパティを編集する（プロパティボタン）

プロパティ画面では \`server.properties\` を次の3段階で編集できます。
- 基本設定: 重要な項目だけを厳選
- 詳細設定: 追加の項目も含めて編集
- 開発者設定: 全項目 + テキスト編集（GUI/テキスト切替）

ポイント:
- テキスト編集では、形式エラーがあると保存できません（行単位でエラー/警告が表示されます）。
- 「デフォルトに戻す」は現在の設定を初期値に戻します。
- 変更が反映されない場合は、停止→起動（再起動）や \`reload\` などが必要なことがあります。

---

## 8. JDK管理

「JDK管理」では、インストール済み JDK を確認できます。
- サーバーで使用中の JDK は「使用中」と表示され、削除できません。
- 基本的にはサーバー作成時に自動で導入されるため、ここはメンテナンス用途です。

---

## 9. ダウンロード管理（高度な操作）

サーバー作成時の自動処理とは別に、手動でダウンロードを試したい場合に使います。
- リスト取得: 「サーバーソフトウェア」または「JDK」を選んで一覧を取得
- ダウンロード開始: バージョン/OS を選んで開始
- 進捗確認: アクティブなダウンロードに速度・残り時間・完了見込みが表示されます
- キャンセル: ダウンロード中のみ停止できます

---

## 10. FRP管理（任意: 外部公開）

FRP管理は、Minecraft サーバーを外部から接続できる形で公開したいときに使います。

### 手順（概要）
1. 「Discordとリンク」で認証を開始します（別タブで認証画面が開きます）。
2. リンク済みになったら、公開したい Minecraft サーバーを選択します。
3. 利用可能な公開ポート・残りセッション枠を確認し、「公開を開始」を押します。
4. 生成された公開 URL を共有して接続してもらいます。

補足:
- 公開できるポート/枠は、画面に表示される許可設定に従います。
- 停止は「停止」ボタン、完全に片付ける場合は一覧から「削除」を使います。
- 「ログ」からトンネルのログを確認できます。

---

## 11. よくあるトラブル

### サーバーが起動できない / Crash! になる
- メモリ（最大/最小）が過大になっていないか確認してください。
- 「コンソール」でエラーログを確認してください。
- 自動再起動を有効にしている場合、通知ベルに原因が出ることがあります。

### プレイヤーが接続できない
- サーバーが「稼働中」になっているか確認してください。
- ポート番号が正しいか確認してください（デフォルトは \`25565\`）。
- 同一 PC 内のプレイ/公開・LAN 内の接続など、利用環境に応じたネットワーク設定が必要です。

### プロパティが保存できない（開発者設定: テキスト編集）
- 行番号付きのエラー表示を確認し、キー重複や \`key=value\` 形式の崩れを修正してください。

---

## 12. 困ったとき

フォーム横の **?** アイコン（ヘルプ）や、右上の通知ベル（イベント）をまず確認してください。
`;

